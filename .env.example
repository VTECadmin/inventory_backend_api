# Copy to .env and adjust for your environment.

# HTTP server
PORT=3000

# PostgreSQL connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=vtec_dashboard
DB_USER=postgres
DB_PASSWORD=

# JWT signing secret (temporary auth; to be replaced by Cognito token validation)
JWT_SECRET=change-me
